;-------------------------------------------------
;2匕首
;编号2
;-------------------------------------------------

;装备类型
;0为武器 1为上身护甲 2为下身护甲
@WEAPON_STYLE_2
RETURN 0

;装备星级
@WEAPON_RANK_2
RETURN 1

;返回装备名字，装备名字必须小于等于4个字
@WEAPON_NAME_2
VARSET TEXT_RETURN
TEXT_RETURN:0 = ひだり
RETURN

;装备介绍
;返回使用的行数
;ARG为装备的レベル
@WEAPON_INTRO_2,ARG
VARSET TEXT_RETURN
;开头的引用与引用者
TEXT_RETURN:0 = 「使うとき、気を付けて」
TEXT_RETURN:1 = 鍛冶師

TEXT_RETURN:2 = ▶随時携帯できる小型の短剣、または小刀、果物切りや食卓用ナイフなど、使用者の要望に応じて変化しますが、常に狭い範囲を離れることはありません。
TEXT_RETURN:3 = ▶鋭器系の最初級武器として進化の可能性を秘めており、他の武器との鍛冶を続けることで、より強力な武器に進化するかもしれません。
TEXT_RETURN:4 = ▶モンスターにとっては小さすぎて致命的なダメージを与えるのは難しいかもしれませんが、敏捷な使用者には適しているかもしれません。
RETURN 5

;装备效果介绍
;返回使用的行数
;装备的效果实时计算
@WEAPON_SKILL_INTRO_2
VARSET TEXT_RETURN
TEXT_RETURN:0 = ▶攻撃力が5パーセント増加。
RETURN 1

;是否可以通过购买或者奖励卡片获得
@WEAPON_GET_FROM_2
RETURN 1

;是否地宫中只能存在一个
@WEAPON_ONLY_2
RETURN 0

;---------------------------------------
;装备效果发动 ARG:0为目标编号，根据情况不同可能为モンスター的编号或者目标勇者的编号
;ARG:1为发动状态 -1为モンスター卸下装备时对モンスター产生效果，0为モンスター装备上装备时产生效果
; 1为当勇者进场或者经过时产生效果 2为每次モンスター受到攻击时触发的效果 3为每次目标受到攻击时触发的效果 4为目标每次被赋予特殊状态时发动效果
;ARG:2为赋予的特殊状态种类
@WEAPON_ACT_2,ARG:0, ARG:1, ARG:2
#DIM LCOUNT 

SELECTCASE ARG:1
    CASE 0
ENDSELECT
RETURN 

