;-------------------------------------------------
;1棍棒
;编号1
;-------------------------------------------------

;装备类型
;0为武器 1为上身护甲 2为下身护甲
@WEAPON_STYLE_1
RETURN 0

;装备星级
@WEAPON_RANK_1
RETURN 1

;返回装备名字，装备名字必须小于等于4个字
@WEAPON_NAME_1
VARSET TEXT_RETURN
TEXT_RETURN:0 = 棍棒
RETURN

;装备介绍
;返回使用的行数
;ARG为装备的レベル
@WEAPON_INTRO_1,ARG
VARSET TEXT_RETURN
;开头的引用与引用者
TEXT_RETURN:0 = 「俺の一突きを受けろ」
TEXT_RETURN:1 = 棒を振り回すモンスター

TEXT_RETURN:2 = ▶これは非常に普通に見える棒で、使用者の思いに応じてサイズが変化する以外に特筆すべき点はありません。
TEXT_RETURN:3 = ▶これは鈍器系の初級武器として進化の可能性を秘めており、他の武器と組み合わせて鍛冶を繰り返すことで、より強力な武器に進化するかもしれません。
TEXT_RETURN:4 = ▶異なる使用方法によって、侵入してくる勇者に対して通常の打撃で気絶させることができます。
RETURN 5

;装备效果介绍
;返回使用的行数
;装备的效果实时计算
@WEAPON_SKILL_INTRO_1
VARSET TEXT_RETURN
TEXT_RETURN:0 = ▶攻撃力が5パーセント増加。
RETURN 1

;是否可以通过购买或者奖励卡片获得
@WEAPON_GET_FROM_1
RETURN 1

;是否地宫中只能存在一个
@WEAPON_ONLY_1
RETURN 0

;---------------------------------------
;装备效果发动 ARG:0为目标编号，根据情况不同可能为モンスター的编号或者目标勇者的编号
;ARG:1为发动状态 -1为モンスター卸下装备时对モンスター产生效果，0为モンスター装备上装备时产生效果
; 1为当勇者进场或者经过时产生效果 2为每次モンスター受到攻击时触发的效果 3为每次目标受到攻击时触发的效果 4为目标每次被赋予特殊状态时发动效果
;ARG:2为赋予的特殊状态种类
@WEAPON_ACT_1,ARG:0, ARG:1, ARG:2
#DIM LCOUNT 

SELECTCASE ARG:1
    CASE 0
ENDSELECT
RETURN 

